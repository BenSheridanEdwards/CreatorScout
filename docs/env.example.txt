# Scout Environment Configuration
# ================================
#
# Copy this file to .env and fill in your values:
#   cp docs/env.example.txt .env
#
# NEVER commit .env to version control!

# ═══════════════════════════════════════════════════════════════════════════════
# DATABASE
# ═══════════════════════════════════════════════════════════════════════════════

# PostgreSQL (recommended for production)
DATABASE_URL="postgresql://scout:your_password@localhost:5432/scout?schema=public"

# SQLite (for development/simple deployments)
# DATABASE_URL="file:./scout.db"

# ═══════════════════════════════════════════════════════════════════════════════
# OPENAI API (for bio analysis)
# ═══════════════════════════════════════════════════════════════════════════════

OPENAI_API_KEY="sk-..."

# ═══════════════════════════════════════════════════════════════════════════════
# RESIDENTIAL PROXY (Decodo) - IMPORTANT: This costs money per GB!
# ═══════════════════════════════════════════════════════════════════════════════

# Decodo credentials
DECODO_USERNAME="your_username"
DECODO_PASSWORD="your_password"
DECODO_HOST="dc.decodo.com"
DECODO_PORT="10000"

# Sticky session duration (minutes) - longer = more consistent but less rotation
DECODO_STICKY_SESSION_MIN="20"
DECODO_STICKY_SESSION_MAX="30"

# Legacy: Smartproxy (if using instead of Decodo)
# SMARTPROXY_USERNAME=""
# SMARTPROXY_PASSWORD=""
# SMARTPROXY_HOST="gate.smartproxy.com"
# SMARTPROXY_PORT="7000"

# ═══════════════════════════════════════════════════════════════════════════════
# ADSPOWER BROWSER PROFILES
# ═══════════════════════════════════════════════════════════════════════════════

# AdsPower API key (for managing browser profiles)
ADSPOWER_API_KEY="your_api_key"

# AdsPower API URL (usually local)
ADSPOWER_API_URL="http://localhost:50325"

# ═══════════════════════════════════════════════════════════════════════════════
# SCHEDULER CONFIGURATION
# ═══════════════════════════════════════════════════════════════════════════════

# Enable automatic session scheduling
SCHEDULER_ENABLED="true"

# Timezone for session times (important!)
SCHEDULER_TIMEZONE="Europe/London"

# Catch up missed sessions on startup
SCHEDULER_CATCHUP="true"

# Maximum retry attempts for failed sessions
SCHEDULER_MAX_RETRIES="2"

# ═══════════════════════════════════════════════════════════════════════════════
# SERVER CONFIGURATION
# ═══════════════════════════════════════════════════════════════════════════════

# API server port
PORT="4000"

# Node environment
NODE_ENV="production"

# ═══════════════════════════════════════════════════════════════════════════════
# DEVELOPMENT OPTIONS
# ═══════════════════════════════════════════════════════════════════════════════

# Use local browser instead of AdsPower (for development)
# LOCAL_BROWSER="true"

# Enable debug screenshots (uses more disk space)
# DEBUG_SCREENSHOTS="true"

# Dry run mode (no actual DMs sent)
# DRY_RUN="true"
